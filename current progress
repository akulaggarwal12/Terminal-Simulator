#include <iostream>
#include <filesystem>
#include <fstream>
#include <string>

using namespace std;
namespace fs = std::filesystem;

int main() {
    string inputPath;
    string outputPath;
    
    int choice;
    cout << "Enter 1 for Copy, 2 for Move: ";
    cin >> choice;
    
    cout << "Enter the File Path: ";
    getline(cin, inputPath);
    
    cout << "Enter the Location Path: ";
    getline(cin, outputPath);
    
    fs::path location(outputPath);
    fs::path source(inputPath);
     
    if (!fs::is_directory(location)) {
        cout << "Location must be a Directory: ";
        return 0;
    }
    
    switch (choice) {
        case 1: {
            
        }
        
        case 2: {
            if (fs::is_directory(source)) {
            
            }
        }
        
        default: 
            cout << "Input a valid choice: ";
    }
         
    return 0;
}
